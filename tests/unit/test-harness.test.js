import { describe, expect, it } from 'vitest';

describe('unit test harness', () => {
  it('runs JavaScript unit tests independently of Phaser rendering', () => {
    expect(true).toBe(true);
  });
});
