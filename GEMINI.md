# Pixel Run & Leap 專案規範

本檔案與 `CLAUDE.md`、`GEMINI.md` 必須保持內容完全同步。修改專案規範時，三個檔案必須一起更新。

## 專案目標

- 建立私人本機使用的 HTML5 2D 遊戲 `Pixel Run & Leap`。
- 參考 `ffx0s/mario-html5` 的遊戲內容與素材。
- 使用 Phaser 3 + JavaScript。
- 支援桌面鍵盤與手機觸控操作。
- 保留像素風格，但使用 `Pixel Run & Leap` 自有名稱與介面。

## 技術規範

- 遊戲核心使用 Phaser 3。
- 使用 JavaScript，不要未經確認改用 TypeScript 或 React。
- 遊戲邏輯與 HTML/CSS 外層介面保持清楚邊界。
- 玩家、敵人、道具、磚塊、場景與系統依功能拆分，避免所有邏輯集中於單一檔案。
- 桌面與觸控輸入共用同一套遊戲狀態與規則。

## 程式碼品質與工具

- 使用 Prettier 統一 JavaScript、JSON 與 Markdown 格式。
- 使用 ESLint 檢查 JavaScript 錯誤與未使用變數。
- 使用 `.editorconfig` 統一縮排、換行與檔案編碼。
- 優先啟用 VS Code 儲存時格式化與 ESLint 修正。
- 不要手動調整格式化工具會自動管理的排版，除非有明確例外。
- 使用 `package.json` 的 npm scripts 執行格式化、Lint、測試與建置。

## 素材與授權

- 圖片、地圖、音樂與音效使用參考專案資產。
- 本專案限定私人本機使用。
- 不得把參考專案的 MIT 程式碼授權解讀為 Mario 素材可公開散布。
- 若未來要公開部署，必須重新確認每項素材的著作權與授權條件，必要時替換素材。
- 保留素材來源與原始檔名，不要任意覆蓋來源資訊。

## 開發流程

- 先檢查既有程式碼、規格與 git 狀態，再修改檔案。
- 遵循最小且直接的變更，不做無關重構。
- 修改前先明確確認需求、成功條件與不在範圍內的內容。
- 不要在尚未確認設計或規格前開始實作大型功能。
- 使用 `apply_patch` 進行手動檔案修改。
- 不要覆蓋或還原其他協作者未建立的變更。

## 遊戲測試與 AI 輔助

實作時應提供開發測試模式，讓 AI 能透過瀏覽器操作並驗證結果：

- `?debug=1` 顯示碰撞框、FPS、座標與遊戲狀態。
- `?test=1` 使用固定初始狀態與可重現關卡。
- 提供測試用快速傳送、無敵、補生命與跳過關卡能力。
- 發生錯誤時輸出清楚的 Console 訊息。
- 支援瀏覽器自動化測試與截圖驗證。
- 正式遊戲預設關閉測試模式。

單元測試使用 Vitest，優先覆蓋不依賴 Phaser 畫面渲染的純 JavaScript 遊戲規則：

- 玩家移動與跳躍狀態。
- 生命、分數、金幣與倒數計時。
- 受傷、死亡、重新開始與通關狀態。
- 道具效果、碰撞計算與關卡資料解析。
- 鍵盤與觸控輸入轉換。

每個新規則先建立會失敗的測試，再寫最小實作讓測試通過。Phaser 場景整合與實際畫面另以瀏覽器測試驗證。

至少驗證以下流程：

- 啟動與資源載入。
- 開始遊戲、移動與跳躍。
- 碰撞、受傷、死亡與重新開始。
- 收集金幣與道具。
- 通關與返回標題。
- 音樂、音效開關與手機觸控。

## 完成前檢查

- 執行專案配置的建置或測試命令。
- 執行單元測試命令並確認沒有失敗或未處理的錯誤。
- 執行格式檢查與 ESLint，確認沒有未處理的錯誤。
- 執行 Playwright 瀏覽器 smoke test。
- 檢查桌面與手機尺寸的操作。
- 檢查瀏覽器 Console 是否有錯誤。
- 執行 `git diff --check`。
- 回報已執行的驗證與尚未驗證的部分，不要宣稱未執行的測試已通過。

## 規範同步

`AGENTS.md`、`CLAUDE.md`、`GEMINI.md` 必須逐字一致。完成任何一份的修改後，立即同步另外兩份，並用檔案比較確認沒有差異。
